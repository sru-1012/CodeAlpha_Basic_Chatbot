# CodeAlpha_Basic_Chatbot
A simple rule-based chatbot built in Python. It takes user input and returns predefined responses using if-elif statements, functions, and a while loop for continuous conversation. Includes a basic typing effect for responses.
